# 어드민 고도화 로드맵 제안 (2026-07-06)

> 종훈님 지시("쓸모없는 기능 제거 / 필요한데 없는 기능 추가")에 대한 **제안서**.
> 임의 삭제 금지 원칙에 따라, 감사(`docs/audit/admin_audit_2026-07.md`) 근거로
> [제거 후보 / 신규 필요 / 우선순위 / 예상 작업량]만 정리. **실행은 종훈님 승인 후 별도 세션.**
> 우선순위: P0=지금 안 하면 위험/기능불능, P1=효용 큼, P2=있으면 좋음.

---

## 1. 이번 세션에서 이미 처리한 것 (참고)
- ✅ [P0] 어드민 401 무한로딩 — 백엔드 토큰 허용셋 수정(라이브 검증 완료).
- ✅ [P0] 방문자 4지표 허수 — 백엔드 정확집계 + KST + 경로한글 + 유입채널 + 툴팁 + PostgREST 1000행 상한 페이지네이션.
- ✅ [P1] **UTM 캠페인 유입 집계** — `20260706_visitor_utm.sql` 프로덕션 적용(Supabase MCP) + 프론트 캡처 + 백엔드 집계 + 어드민 캠페인 섹션. 엔드투엔드 실측 완료(2026-07-06).
- ✅ [P1] **전환 퍼널(방문→가입→계산)** — visitor-stats에 퍼널 집계 + 어드민 3단계 카드(전환율·툴팁).
- ⏸️ [P1] 어드민 JWT 인증 전환 — **미실행(의도적 보류)**: 잘못 전환 시 어드민 전면 잠김 위험 → 별도 세션에서 X-Admin-Token 병행 수용→점진 전환 권장(§4 가드레일).
- 🔎 제거 후보 `markTemplateUsed` → **죽은 코드 아님 확인**(InquiryDetailPanel.tsx:154 실호출). 제거 안 함.

---

## 2. 신규 필요 기능 (없어서 아쉬운 것)

| 우선 | 기능 | 왜 필요 | 예상 작업량 | 선행조건 |
|------|------|---------|-------------|----------|
| **P1** | 어드민 인증을 Supabase JWT 검증으로 전환 | 현 `X-Admin-Token`은 공개값(비밀 아님) — 아는 사람은 어드민 API 직접 호출 가능. 실질 보안이 약함 | 중(백엔드 의존성 주입 + 프론트 세션토큰 전달, 3~5개 파일) | 없음 |
| **P1** | UTM 캠페인 유입 집계 | 광고/제휴 유입을 채널별로 정확히 보려면 referrer만으론 부족(카카오 인앱 등은 referrer 소실) | 소~중(마이그레이션 1 + 프론트 insert + 백엔드 집계 + 어드민 섹션) | `20260706_visitor_utm.sql` DB 적용(1클릭) |
| **P1** | 방문→가입→계산 전환 퍼널 위젯 | 지금은 방문·가입·리포트가 따로 놀아, "방문자 중 몇 %가 계산까지 갔나"를 못 봄. B2C 성장 핵심 지표 | 중(백엔드 조인 집계 + 차트 1) | visitor_logs·profiles·reports |
| **P2** | 일자별 방문자 추이 라인차트 | 현재 방문자 탭은 스냅샷만, 시계열 추세가 없음(가입/리포트만 analytics에 있음) | 소(백엔드 by_date에 visitor 추가 + 기존 차트 재사용) | 없음 |
| **P2** | 실시간 접속자(최근 5분) 배지 | 운영 중 "지금 몇 명 보고 있나" 확인 | 소 | 없음 |
| **P2** | 이탈/재방문(세션 재등장) 지표 | 세션ID 기반 재방문율은 리텐션 파악에 유용 | 중 | 없음 |

## 3. 제거/정리 후보 (임의 삭제 X — 승인 후)

| 우선 | 대상 | 근거 | 처치 제안 |
|------|------|------|-----------|
| P2 | `api.ts` 미사용 함수(`markTemplateUsed`/`postAuditLog` 등) | 정의는 있으나 호출부 불명(감사 §B-2) | grep로 소비처 0 재확인 후 제거 or 연동 |
| P2 | JobPostings/Accounts의 Supabase 직접 CRUD | RLS 세션 의존(감사 §B-2 #4) | 백엔드 service-role 경로로 통일(다른 메뉴와 일관) |
| P2 | 개요/방문자 탭 빈상태 UI | "데이터 없음" 안내 미흡 | 공통 Empty 컴포넌트 적용 |
| P2 | Target 메뉴 vs 대시보드 지표 중복 | 조회 전용 인사이트가 여러 곳에 흩어짐 | 정보구조 통합 검토(신중히) |

## 3-b. 운영 가이드 (UTM 사용법 · 리뷰어 B 권고)
- **캠페인 링크는 리다이렉트 없는 공개 경로에 UTM을 붙일 것**. 예: `https://catch-daily-worker.vercel.app/?utm_source=kakao&utm_campaign=여름` (루트 `/`=랜딩, 리다이렉트 없음) 또는 `/guide`, `/severance` 등. `/home`은 비로그인 시 `/`로 리다이렉트되며 그 과정에서 UTM이 유실될 수 있음(첫 진입 캡처 실패).
- **utm_source 는 소문자 권장**(백엔드가 소문자 정규화하므로 `Kakao`·`kakao` 동일 집계).
- **anon UTM 스팸(별도 트랙)**: `visitor_logs` INSERT는 설계상 anon 허용(비로그인 방문 기록용)이라 누구나 임의 utm 행을 넣을 수 있음(기존 위험, 이번 변경이 신규 도입 아님). 캠페인 통계 신뢰도가 중요해지면 rate-limit 또는 서버 프록시 수집으로 하드닝 검토.

## 4. 하지 말아야 할 것 (가드레일)
- 계산 로직(28일 블록 등) 절대 무변경.
- `visitor_logs` insert에 utm 필드 추가는 **마이그레이션 적용 확인 후에만**(안 그러면 방문기록 끊김).
- `admin_accounts`/RLS 정책은 운영 데이터라 변경 전 반드시 실측·승인.
- `X-Admin-Token` 제거는 JWT 전환(P1)과 **동시에** — 먼저 지우면 어드민 전면 잠김.

## 5. 권장 실행 순서 (승인 시)
1. UTM DB 적용(1클릭) → 프론트 insert + 백엔드 집계 + 어드민 UTM 섹션 (P1, 반나절)
2. 전환 퍼널 위젯 (P1, 반나절)
3. 어드민 JWT 인증 전환 (P1, 하루) ← 보안 핵심
4. 방문자 추이 차트 + 실시간 배지 (P2, 반나절)
5. 정리(미사용 함수·직접 CRUD 백엔드화·빈상태) (P2, 반나절)
